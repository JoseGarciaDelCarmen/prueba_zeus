import { FiltroGruposPipe } from './filtro-grupos.pipe';

describe('FiltroGruposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGruposPipe();
    expect(pipe).toBeTruthy();
  });
});
